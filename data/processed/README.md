# Processed Data

This directory contains preprocessed datasets ready for training.

Files in this directory are gitignored (generated from raw data).

Generate processed datasets using:
```bash
pixi run prepare-data
```
