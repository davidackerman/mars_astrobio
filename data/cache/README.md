# Cache

This directory contains cached HTTP responses from the NASA PDS API.

Files in this directory are gitignored (can be regenerated).

The cache helps avoid re-downloading the same data multiple times.
