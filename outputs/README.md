# Outputs Directory

This directory contains generated outputs from experiments and inference.

## Structure

- `predictions/` - Prediction results from batch inference (gitignored)
- `visualizations/` - Generated plots and figures (gitignored)
- `reports/` - Analysis reports (gitignored)

## Notes

Output files are gitignored but can be regenerated from models and data.
